{
  "name": "TEMP",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build:clean": "rmdir /s /q dist && tsc",
    "serve": "nodemon ./src/dev/app.ts",
    "setup-dev": "npm i -D nodemon express cors @types/express @types/cors typescript"
  }
}

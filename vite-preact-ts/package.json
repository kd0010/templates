{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "setup": "npm i preact && npm i -D @preact/preset-vite sass typescript vite"
  },
  "dependencies": {
    "preact": "latest"
  },
  "devDependencies": {
    "@preact/preset-vite": "latest",
    "sass": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
